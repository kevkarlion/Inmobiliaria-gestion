export enum ClientSource {
  WEB = "web",
  REFERRAL = "referido",
  PUBLISHED = "publicado",
  OFFICE = "oficina",
  OTHER = "otro",
}

export type ClientSourceValue = "web" | "referido" | "publicado" | "oficina" | "otro";
