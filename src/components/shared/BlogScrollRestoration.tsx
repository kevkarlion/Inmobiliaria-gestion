"use client";

// Ya no es necesario: la navegación entre categorías ahora usa searchParams
// (misma página, sin transición de ruta), por lo que el scroll se mantiene naturalmente.
export default function BlogScrollRestoration() {
  return null;
}
